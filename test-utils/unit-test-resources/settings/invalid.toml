environment = "development"

[database]
host = "localhost"
port = "not-a-port"
user = "appuser"
name = "appdb"
password_file = "test-utils/secrets/db_password_with_newline.txt"
max_connections = 10
connect_timeout_secs = 5
ssl_mode = "disable"
